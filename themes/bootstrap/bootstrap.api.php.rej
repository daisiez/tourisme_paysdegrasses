--- Breadcrumb.php
+++ Breadcrumb.php
@@ -40,6 +40,8 @@ public function preprocess(array &$variables, $hook, array $info) {
           'text' => $page_title,
           'attributes' => new Attribute(['class' => ['active']]),
         ];
+        // Add cache context based on url.
+        $variables['#cache']['contexts'][] = 'url';
       }
     }
 
